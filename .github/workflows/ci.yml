name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:
  # The fuzz lane is the only scheduled work. A parser is worth re-fuzzing
  # because the corpus grows, not because the code changed.
  schedule:
    - cron: "37 4 * * 1"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

env:
  CARGO_TERM_COLOR: always
  # tmux emits bytes rather than text, and the fixtures assert on wording.
  LC_ALL: C.UTF-8

jobs:
  # Everything `just check` runs: formatting, Clippy, tests, doctests, docs,
  # the feature powerset, cargo-deny, both MSRV floors, and packaging.
  check:
    name: just check
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7

      - name: Install tmux
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends tmux
          tmux -V

      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy, rustfmt

      # `just msrv` invokes these by exact name via `rustup run`. Two floors,
      # because they are two promises: the libraries support 1.85, and
      # tmux-mcp cannot, since its dependencies require 1.88.
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: "1.85.0"
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: "1.88.0"

      - uses: taiki-e/install-action@v2
        with:
          tool: just,cargo-hack,cargo-deny

      - uses: Swatinem/rust-cache@v2

      - run: just check

  # tmux's habitat is not only Linux, and the parts most likely to differ are
  # exactly the ones this crate leans on: process groups, Unix sockets,
  # `waitid`, and temporary paths. Scoped to the test suite rather than the
  # whole gate, and to master rather than every push, because a macOS runner
  # bills at ten times a Linux one and the lints are platform-independent.
  macos:
    name: tests on macOS
    if: github.ref == 'refs/heads/master' || github.event_name == 'workflow_dispatch'
    runs-on: macos-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - name: Install tmux
        run: |
          brew install tmux
          tmux -V

      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      - uses: Swatinem/rust-cache@v2

      - run: cargo test --locked --workspace --all-targets --all-features

  # There is no automated semver gate while every version is a prerelease:
  # `cargo-semver-checks` skips every lint on a prerelease-to-prerelease step
  # and then reports success. This does not judge whether a change is allowed,
  # it says one happened, which is what review needs.
  api:
    name: public API surface
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - uses: Swatinem/rust-cache@v2
      - run: just api-check
      # Same toolchain and the same rustdoc JSON, so it rides along here
      # rather than paying for a second nightly build.
      - run: just example-coverage-check

  # A short fuzz pass over the parsers that read from outside the process. Kept
  # brief and off the pull-request path: this finds things by running for a
  # long time, so a weekly schedule is worth more than a gate nobody can wait
  # for.
  fuzz:
    name: fuzz parsers
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        target: [control_line, filter_expr_json, workspace_yaml]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-fuzz
      - uses: Swatinem/rust-cache@v2
      - name: Fuzz ${{ matrix.target }}
        env:
          TARGET: ${{ matrix.target }}
        run: |
          # cargo-fuzz builds for the triple it was itself compiled for, and
          # the installer above ships a musl binary. musl links libc
          # statically, which a sanitizer cannot instrument, so the target
          # has to be named here rather than left to how cargo-fuzz arrived.
          host=$(rustc +nightly -vV | sed -n 's/^host: //p')
          cargo +nightly fuzz run --target "$host" "$TARGET" "fuzz/seeds/$TARGET" \
            -- -max_total_time=300 -rss_limit_mb=4096
      - name: Keep any crash that was found
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: fuzz-artifacts-${{ matrix.target }}
          path: fuzz/artifacts/

  # The slow lane. tmux's output and error wording change between releases, so
  # every supported one is built from source and runs the whole workspace.
  compat:
    name: tmux compatibility
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7

      - name: Install tmux build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            bison \
            build-essential \
            libevent-dev \
            libncurses-dev \
            pkg-config

      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      - uses: taiki-e/install-action@v2
        with:
          tool: just
      - uses: Swatinem/rust-cache@v2

      - run: just compat
